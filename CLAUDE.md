# 환자군(NHIS-HCC) 기반 일차의료 지불모형 시뮬레이터

국민건강보험 일산병원 일차의료개발센터. 정책 시뮬레이션 도구.

**정책 기준**: 노션 「일차의료 시범사업 지불체계 보완 방안 v2.7」 (2026-04-19)
**시뮬레이터 버전**: **v6.1.0** (2026-04-19 main 머지 · 정책 v2.7 반영)

## 기술 스택

- React 18 + Vite 6 · Tailwind CSS 3
- Recharts (차트) · SheetJS/xlsx (엑셀 I/O)
- Vitest + @testing-library/react (단위 테스트)
- 배포: GitHub → Vercel 자동 배포 (main 브랜치 = production)

## 개발 · 운영 명령

```bash
npm install
npm run dev       # 개발 서버 (http://localhost:5173)
npm run build     # 프로덕션 빌드 (dist/)
npm run preview   # 빌드 결과 로컬 확인
npm test          # 단위 테스트 (vitest run)
```

## 도메인 모델 · 핵심 수식

### 용어 (노션 v2.7 기준, 준수 필수)

| 기호 | 의미 | 단위 |
|---|---|---|
| P | 환자군 기본수가 = 환자군 기준의료비 × 의원급 외래비중 | 원/년/환자 |
| F | **일차의료 기능수가** (환자군별 배열 F_g[4]) — 주치의 등록관리 + 저평가된 본연 기능의 상대가치 보정 | 원/년/환자 |
| **T** | **통합 수가 = P + F (명목 청구수가)** | 원/년/환자 |
| **A** | **공단 실지급 = P × (1 − L) + F** | 원/년/환자 |
| B | 본인부담 = M1 × 30% (고정) | 원/년/환자 |
| L | 타원이용비중 (비용 기반, (C−M)/C) | 0~1 |
| LC | L 변화율 (정책 시뮬 슬라이더) | %p |
| M1 | 1인당 현행 등록의원 외래비 | 원/년 |
| N | 실인원 환자수 (연인원 아님) | 명 |
| M | 의원 수 | 개 |
| n_reg | 의원당 등록환자수 = Σ regDist | 명 |
| regDist | 의원당 환자군별 등록환자수 배열 (기본 [100,600,200,100]) | 명 |

**F는 L 우회**: F는 타원이용비중 L에 걸리지 않고 등록의원에 고정 지급. 공단 실지급 수식에서 F는 (1−L) 곱셈 밖에 위치.

**F의 정의 (v2.7)**: F는 행위별 수가에 얹는 add-on이 아니라, **환자군 기반 지불 구조 내부에 내장된 기능 상대가치 재조정 항목**. 주치의 등록관리 업무와 저평가된 일차의료 본연 기능(만성질환 포괄관리·재택의료·건강상담)의 상대가치를 동시에 보정.

### 혼합 수입 수식

환자군 g에서 발생하는 의원 수입:
- 등록환자 (환자군 모형): `(P × (1−L) + F_g + M1 × 0.3) × n_reg_g`
- 비등록환자 (FFS 유지): `M1 × n_unreg_g`
- 의원 총 수입 = 위 두 합계

### Track 재해석 (v2.7 · 노션 Q3 "수입 감소 없음" 준수)

- Track A = FFS + F
- Track C = 환자군 모형 + F (LC 적용)
- Track B = A와 C의 hccPct 가중평균
- **모든 Track에서 등록환자에게 F 가산** (핵심 원칙)
- 비등록환자는 Track과 무관하게 항상 FFS
- Track 변화율 기준선 = 순수 FFS (T.inc0). Track A도 F 효과로 양(+) 변화.

### PT (일차의료 전환지원금) — 시뮬레이터 수식과 독립

- 1회성 · 첫해만 지급, 의원당 기준
- Track A 미지급 / Track B 1,500만원 / Track C 3,000만원
- hccPct 선형 보간: `PT = (hccPct / 100) × 30,000,000`
- 시뮬레이터 계산(의원 수입·공단 지출)에 포함되지 않음 (정보 표시만)

## 파일 구조

```
src/
├── App.jsx                      # 탭 라우팅
├── main.jsx                     # 엔트리
├── constants.js                 # SH, CL, INIT_BASE, INIT_P, INIT_F, INIT_REG_DIST,
│                                # INIT_M_CLINICS, INIT_TOTAL_N, INIT_DATA_LABEL, ON
├── utils.js                     # f, fE, fAuto, fMan, pct, diffAuto, diffMan
├── hooks/useSimulator.js        # 전역 상태·계산 (useReducer + useMemo)
├── components/
│   ├── Header.jsx
│   ├── DatasetSelector.jsx
│   ├── TabSimulation.jsx        # 탭 1: 수가 시뮬레이션
│   ├── TabTrack.jsx             # 탭 2: Track A/B/C
│   ├── TabSharedSaving.jsx      # 탭 3: Shared Saving
│   ├── RegistrationPanel.jsx    # named: FCard, TCard, RegScaleCard
│   ├── WinWinWin.jsx
│   └── shared/NumBox.jsx
├── data/presets/
│   ├── index.js
│   └── 2023.json                # 파일럿 데이터 (10개 의원, 69,604명)
└── test/
    ├── calculator.test.js
    └── utils.test.js
```

## UI 시각 위계 (v6.1 레이아웃 — 시연·모바일 최적화)

### 수가 시뮬레이션 탭 순서

1. **등록환자 규모** — 접힘 아코디언 (맨 위, 기본 접힘)
   - 한 줄 요약 상시: `M 100 · 의원당 실인원 1,000 · 등록 1,000 (10%/60%/20%/10%)`
   - 펼치면: 의원당 실인원(프리셋 1000/1500/2000/3000/5000/7000/10000), M(10/100/1000/3000), 의원당 등록(500/1000/1500/2000), 환자군별 분포 (명 + % 양방향 편집)
2. **환자군 기본수가 (P)** — 4 슬라이더 + NumBox
3. **일차의료 기능수가 (F)** — P와 동일 구조 4 슬라이더 + NumBox, 프리셋 3종(공식안/균등1만원/중증편중)
4. **통합 수가 (T = P + F)** — 접힘 아코디언 (기본 접힘, 결과 참조용)
5. **타원이용비중 (L) 변화율** — 슬림 박스 (헤더 한 줄에 "현재 → 변화 후 · LC%p", 슬라이더만)
6. **KPI 2카드** (토글 없음, 상시 표시)
   - 녹색 (의원 수입 변화):
     - 전체 변화액 (억원, text-3xl) + %
     - 의원당 평균 변화 **만원 단위** (text-3xl, 확대)
     - 기준선 의원당 수입 XXX만원/년 (상시)
     - LC 후 의원당 수입 YYY만원/년 (상시, text-lg)
     - 주석: "의사 1인 소득 ≠ 의원 외래 수입"
   - 파랑 (공단 의원급 외래 지출 변화): 전체 변화액 + % + 의원당 평균 변화
7. 차트 2열 (기존 vs LC 후, 공단 지출 비교)
8. Win-Win-Win
9. **📐 공식 구조** — 접힘 토글 (기본 접힘)
   - T = P + F · A = P × (1 − L) + F · B = M1 × 30% 수식
   - 환자군별 L 현재 → 변화 후 4박스
10. **⚙️ 데이터 관리** — 접힘 (엑셀 업로드/내보내기, 파일럿 데이터 로드, 편집 테이블)

### Track 탭 순서 (수가 탭과 동일 패턴)

1. **Track 선택** (Track A/B/C 버튼 + 행위별↔환자군 슬라이더)
2. **타원이용비중 (L) 변화율** — 슬림 박스 (수가 탭과 동일)
3. **KPI 2카드** (토글 없음, 의원당 수입 상시)
   - Track 수입 변화 · 공단 지출 변화
   - 의원당 평균 변화 만원 단위 확대
   - 기준선 / Track 후 의원당 수입 상시
4. **일차의료 전환지원금 (PT)** 박스 — 황색, 1회성 첫해
   - Track A/B/C 3단 표시, 현 선택 하이라이트
   - 의원당 1년차 합계 = Track 수입 변화 + PT
5. Track별 1인당 실지불액 차트 + 비교표

### Shared Saving 탭 순서 (입력→결과 원칙)

1. **항목별 절감 시뮬레이션** (급성기 입원 · 응급 · 요양병원 슬라이더)
2. **Shared Saving 총괄** (건강보험 총진료비, 총의료비 절감률 마스터 슬라이더, 총 절감액)
3. 절감액 배분 비율 (공단 vs 일차의료)
4. 배분 파이 차트 + Win-Win-Win + 안내

### 시연 흐름 (모바일 세로 화면)

기본 정보(접힘) → F 슬라이더 → L 슬라이더 → KPI가 한 화면 내에 들어오는 구조. 슬라이더 드래그와 KPI 반응이 동시에 보임. 수가/Track 탭 공통 적용.

## 입력값 범위

| 항목 | 슬라이더 범위 | NumBox 범위 | 디폴트 |
|---|---|---|---|
| P (환자군 수가) | 5만~200만원 | ≥ 0 | 22/30/52/74만원 |
| F (환자군별 기능수가) | 0~60만원 | ≥ 0 | **공식안**: 122,860 / 203,040 / 291,120 / 362,530원 |
| regDist (환자군별 등록수) | 슬라이더 없음 (직접 입력) | ≥ 0 | [100, 600, 200, 100] (부록 추정치) |
| LC | -30~0%p | 무제한 | -3%p |
| 의원당 실인원 | 프리셋 1,000·1,500·2,000·3,000·5,000·7,000·10,000 | ≥ 1 | **1,000** (시범사업안) |
| M (의원 수) | 프리셋 10·100·1,000·3,000 | ≥ 1 | **100** (시범사업안) |

## 기본 시나리오 (시뮬레이터 초기값 = 복지부 시범사업안)

- M_clinics = 100 · 의원당 실인원 1,000명 · totalN 100,000명
- 의원당 등록환자 1,000명 (100% 등록 시나리오)
- 환자군별 분포 10/60/20/10 (보고서 부록 추정치; 실측 자료 없음)
- F 환자군별 차등 122,860/203,040/291,120/362,530원 (복지부 공식안)
- L: 파일럿 실측 (평균 78.6% 수준)

이 기본값에서 F 총액 = **228.6억원/년**, 부록 §2와 정확히 정합.

**파일럿 데이터 전환**: 데이터 관리 카드의 "파일럿 로드" 버튼 → `loadPreset(presets[0])`로 2023 파일럿(10기관·69,604명, L·M1 실측값) 로드. F_g·regDist는 유지.

## 금액 포맷 규칙

`utils.js` helpers:
- `fAuto(v)` — 자동 단위 (조/억/만원/원)
- `fMan(v)` — 만원 단위 고정 (의원당 평균처럼 가독성 필요 시)
- `diffAuto(a, b)` · `diffMan(delta)` — 부호 포함 변화량

KPI 카드에서는:
- 전체 변화액 (큰 스케일) → `diffAuto`
- 의원당 평균 변화 (작은 스케일) → `diffMan`
- 의원당 수입 절대값 → `fMan`

## 엑셀 포맷 호환

`NHIS-HCC분석템플릿 v6.xlsx` (`시뮬레이터_출력` 시트 4행 × 6열)
- 헤더: `환자군 / 기준의료비 / 의원비중 / 환자수 / 현재외래비 / 타원이용비중 / 수가`

`src/hooks/useSimulator.js:handleFile` 업로드 처리. F_g·regDist는 엑셀로 이관 안 됨 (UI에서 설정).

## 배포 워크플로

- `main` → Vercel production (https://primary-simulator.vercel.app/)
- `feature/*` push 시 자동 preview URL 생성
- **커밋 이메일은 반드시 GitHub noreply 형식**: `59140997+shleefm@users.noreply.github.com`
- **새 작업은 feature 브랜치 필수** (main 직접 푸시 금지, 사용자 명시 승인 시만 예외)
- main 머지는 `--no-ff` 후 필요 시 버전 태그 부여
- 버전 태그 이력: `v5.0` (pre-R), `v6.0.0` (R 도입), `v6.1.0` (R→F, 정책 v2.7)

## 파비콘·PWA

- `public/favicon.svg` · `icon-192.png` · `icon-512.png` · `manifest.webmanifest`
- SVG 수정 후 PNG 재생성: `npm install --no-save sharp && node scripts/gen-icons.mjs`

## 시뮬레이터 밖 항목 (노션 v2.7 명시, 정보 표시만)

- **등록 인센티브** (본인부담 50% 1회 감면) — 공단 보충, 의원 수입 변동 0
- **일차의료 전환지원금 (PT)** — Track 탭 PT 박스에 표시. 수식 영향 없음
- **거점 일차의료지원센터 운영비** — 10개 × 3억원/년 (지원 기능, UI 외)

## 준수 사항 (정책 일관성)

1. **핵심 수식은 변경 금지**: P, A, B, L, T = P + F
2. **용어 통제**: 위 테이블 용어만 사용. 구버전 용어 "R(등록관리비)", "PP(최종 일차의료수가)", "포괄수가", "실지불액", "참여 지원금" 등 금지.
3. **T ≠ A**: T는 명목 청구수가, A는 공단 실지급. UI에서 둘 다 표기.
4. **F의 L 우회 규칙**: 수식·UI에서 반드시 명시
5. **모든 Track에 F 가산**: 노션 Q3 "수입 감소 없음" 원칙

## 메모리

프로젝트 메모리는 `{user-claude-dir}/projects/C--Users-User-projects-Primary-Simulator/memory/`에 저장. 주요 파일:
- `MEMORY.md` — 인덱스
- `user_role.md` — 사용자 프로필 (일산병원 일차의료개발센터장)
- `project_per_clinic_kpi.md` — 의원당 평균 수입 KPI 원칙
- `project_v6_state.md` — v6.1.0 main 머지 상태
- `feedback_branch_push.md` · `feedback_ui_style.md` · `reference_key_docs.md`
